# Generated by xcodegen — but we still commit the pbxproj so the repo
# builds without xcodegen installed. Drift between project.yml and the
# pbxproj is caught by `xcodegen generate && git diff`.
**/xcuserdata/
**/*.xcuserstate
DerivedData/
build/
*.dSYM.zip
*.dSYM
